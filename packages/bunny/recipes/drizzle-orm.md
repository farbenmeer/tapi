# Drizzle ORM Setup for Bunny

To setup [drizzle-orm](https://orm.drizzle.team/) with bunny you just follow the regular setup instructions for drizzle-orm.

I recommend putting the drizzle schema files in the folder `src/schema` and the drizzle client in `src/lib/db.ts`:
