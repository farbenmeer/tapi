{
  "name": "tapi",
  "private": true,
  "scripts": {
    "build": "bun run --filter './packages/tapi' build && bun run --filter './packages/react-tapi' build",
    "release": "bun run build && bun run --filter './packages/*' release; echo 'Done'"
  },
  "workspaces": [
    "packages/*"
  ]
}
