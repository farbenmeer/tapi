{
  "name": "tapi",
  "private": true,
  "scripts": {
    "build": "for i in {1..4} ; do bun run --filter \"./packages/$i-*\" build ; done",
    "release": "for i in {1..4} ; do bun run --filter \"./packages/$i-*\" release ; done ; echo 'Done'",
    "test": "bun run --filter '*' test",
    "clean": "rm -rf node_modules && rm -rf packages/*/node_modules packages/*/dist"
  },
  "devDependencies": {
    "@changesets/cli": "^2.29.6",
    "typescript": "^5.9.3"
  },
  "workspaces": [
    "packages/*"
  ],
  "trustedDependencies": [
    "@parcel/watcher",
    "@tailwindcss/oxide"
  ]
}
