{
  "name": "tapi",
  "private": true,
  "scripts": {
    "build": "pnpm run -r build",
    "release": "pnpm -r --no-bail release",
    "test": "pnpm run -r test",
    "lint": "pnpm run -r lint"
  },
  "devDependencies": {
    "@changesets/cli": "^2.29.6",
    "@playwright/test": "catalog:",
    "typescript": "^6.0.0"
  },
  "packageManager": "pnpm@11.1.1"
}
