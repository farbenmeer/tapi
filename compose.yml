services:
  redis:
    image: redis:8-alpine
    ports:
      - "6379:6379"
  postgres:
    image: postgres:17-alpine
    environment:
      POSTGRES_PASSWORD: postgres
    ports:
      - "5432:5432"
